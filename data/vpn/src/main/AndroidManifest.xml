<?xml version="1.0" encoding="utf-8"?>
<!--
    Minimal manifest for the :data:vpn library module.

    The <service> declaration for VpnTunnelService, the BIND_VPN_SERVICE permission,
    INTERNET, CHANGE_NETWORK_STATE, and FOREGROUND_SERVICE declarations all belong to
    the :app manifest and are wired in issue #65. This library manifest intentionally
    contains no component entries so that the merge does not accidentally include the
    service before #65 is ready.
-->
<manifest />

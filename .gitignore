*.iml
.gradle
/local.properties
.idea
.DS_Store
/build
build/
/captures
.externalNativeBuild
.cxx
local.properties
CLAUDE.local.md

# Local emulator showcase captures (not versioned)
.showcase-shots/

# Local swarm/planning artifacts (not versioned)
swarm-report/

# CI-built gomobile binding (XrayCore.aar). Never vendored in VCS:
# F-Droid main prohibits prebuilt .aar in libs/ (ADR 0001); the native-build
# job (#72) rebuilds it from the pinned libXray submodule each run.
/data/vpn/libs/

# QA scratch artifacts (screenshots, logcat, uiautomator dumps from device runs)
.qa-tmp/

# Debugging / QA scratch (issue #111) — never commit
/_debug_*.md
/golog*.txt
/g[0-9].txt
/swarm-report/
/*.png
